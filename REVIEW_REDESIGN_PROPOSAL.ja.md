# 提案書: 復習機能の再設計（類似問題生成の廃止）

> 対になる文書: [`REVIEW_REDESIGN_PROPOSAL.vi.md`](./REVIEW_REDESIGN_PROPOSAL.vi.md)
> 会議中に言語をまたいで参照できるよう、見出し番号は両言語版で統一しています。

---

## 0. 背景

現在の復習機能は、以下の設計でした。

> 学習木構造から復習したい知識ノードを選択する → AIがそのノードの類似問題を生成する →
> そのノードが最初に作られた際に使っていたチャット画面に戻る → 同じチャットの中で
> 類似問題についてやり取りする → ヒント数・所要時間を記録する

一方で、このアプリのAIチューターは「ユーザーに質問を投げかけることで、ユーザー自身の
主体的な学習を促す」ことを役割とする方針に定まりました。ユーザーの主体的な「思考」の
確認まではしない（判定が難しいため）、というのが現在の方針です。

類似問題の自動生成・（将来的な）答え合わせ・回答解説といった振る舞いは、この
「質問で促す」という方針とズレており、実装も複雑になってしまいます。

そこで、**復習機能のうち「AIによる類似問題の自動生成」を廃止する**ことを提案します。
一方、「知識ノードを選択 → そのノードと1:1で結びついた過去のチャットに戻る」という導線は
**廃止しません。むしろ正しく機能するようDB設計を見直して残します**（詳細は2-3節）。
これは、復習を簡単にして主体的な学習を後押しするという方針を支える、この提案の
中心的な変更です。

---

## 1. 廃止するものの範囲（認識合わせ）

まず「何を廃止するか」をここで明確にします。

- **1-1.** AIによる類似問題の自動生成（`generate_similar_problem`）
- **1-2.** 生成した類似問題を、新しい知識ノード（`origin_node` 付き）としてDBに保存する仕組み
- **1-3.** 復習開始の**たびに新しいチャットセッションを作ってしまう**現状の挙動
  （※現状、これは実行時エラーで実際には動作していません。`start_review()` が
  存在しない `chat.services.start_attempt` を呼んでおり、`reviews/views.py` も
  存在しない `attempt.chat_session` を参照しています）。これは廃止しますが、
  代わりに「同じ知識ノードなら常に同じ永続チャットに戻る」導線に置き換えます
  （新規作成をやめるだけで、過去チャットに戻るという導線自体は残します）。
- **1-4.** 類似問題に対する答え合わせ・回答解説機能（未実装だが、当初の構想にはあった）

一方、「学習した内容をいつ復習すべきか」を計算する間隔反復（SM-2）のスケジューリング
**自体は今回の廃止対象に含めない**という理解で以下を書いています。この前提が違う場合は
指摘してください。

---

## 2. DB設計への影響

### 2-1. 削除するもの

| 対象 | 理由 |
|---|---|
| `KnowledgeNode.origin_node`（自己参照FK） | 類似問題ノードの出自を示すためだけの用途で、他の使い道がない |
| `topics.services.create_derived_node()` | 類似問題ノードを作る関数。呼び出し元がなくなる |
| `topics.services.build_learning_tree()` 内の `origin_node__isnull=True` 絞り込み | 全ノードが常設ノードになるため、絞り込み自体が不要になる |
| `reviews.services.generate_similar_problem()` | 類似問題生成そのものを廃止するため |
| `reviews.services.resolve_schedule_node()` | `node.origin_node or node` という判定が不要になる |

### 2-2. そのまま残すもの

- `ReviewSchedule`（`easiness_factor` / `interval_days` / `repetitions` /
  `next_review_at` / `last_learned_at` / `learned_count`、および
  `retention_level` / `retention_color`）— 「いつ復習すべきか」の計算は今回の
  廃止範囲と無関係なので維持
- `apply_sm2()` / `rating_from_hint_count()` — アルゴリズム自体は変更なし

### 2-3. 変更が必要なもの

以下は§0の方針（復習を簡単にして主体的な学習を後押しする）を技術的に支える、
この提案の中心的な変更です。

- **`ChatSession.knowledge_node`**: 通常の `ForeignKey` → `OneToOneField` に変更。
  1つの知識ノードに対してチャットセッションが常に1つだけになるようにし、
  「そのノードを作った時のチャットに戻る」という導線を成立させます。
- **`hint_count` / `completed_at` の置き場所**: 現在は `ChatSession` に直接
  持たせていますが、1つのチャットセッションを何度も復習に使い回す以上、
  「何回目の復習か」を区別できる形で記録する必要があります。そのため、
  `ChatSession` に対して1:Nとなる「1回分の学習・復習の記録」を表す実体
  （仮称 `Attempt`）を新設し、`hint_count` / `completed_at` はそちらに
  持たせることを提案します。
  - `Attempt`: `chat_session`（FK, `ChatSession` 1件に対してN件）、
    `hint_count`、`completed_at`
- **`ReviewLog.attempt`**: 現在は空テーブル同然の旧 `Attempt`
  （`chat.ChatSession` のエイリアス）を指していますが、上記で新設する
  `Attempt` を指すよう変更します。

### 2-4. 【要チーム確認】オープンな論点

- **論点A — `Attempt` テーブルの新設要否**:
  SM-2は「同じ内容を繰り返し思い出せたか」を複数回にわたって評価するアルゴリズムです。
  ヒント数・完了時刻を `ChatSession` に直接持たせたままだと、2回目の復習で1回目の
  記録が上書きされてしまい、正しく機能しません。この理由から `Attempt` の新設を
  提案していますが、他に良い代替案があれば議論したいです。
- **論点B — `ChatMessage` と `Attempt` の紐付け**:
  「どの `Attempt`（何回目の復習）での発言か」を `ChatMessage` に持たせるかどうか。
  UI上で「1回目の学習」「2回目の復習」を視覚的に分けたい場合は必要になりますが、
  優先度が高くなければ後回し（Phase 2）でも問題ありません。
- **論点C — 復習の完了基準**:
  「復習を完了した」とみなす基準をどうするか。過去のチャットを開いて見ただけで
  OKとする（受動的）か、その中で何かしら操作をして初めてOKとする（能動的。例:
  メッセージを送る、AIの問いに答える）か。前者は手軽ですが「開いただけ」がSM-2の
  評価対象になり得ます。後者は0節の「質問による主体的学習の促進」という方針と
  より整合しますが、具体的にどの操作を要求するかを決める必要があります。この
  基準は `Attempt.completed_at` をいつ記録するかに直結します。

---

## 3. 現状実装で「そのままでよい部分」と「変える必要がある部分」

### 3-1. そのままでよい部分

- `apps/chat/services.py` の `SYSTEM_PROMPT`（ヒントを小出しにし、直接の答えを
  言わないAIチューターとしての振る舞い）— むしろ今回の方針と一致しているため
  変更不要
- `ChatMessage` の分岐推定機能（`parent_message` / `suggested_parent` /
  `parent_confidence` など）— 今回の話とは独立した機能のためそのまま
- `Topic` モデル・学習木のトピック階層部分 — 無関係のためそのまま
- フロントエンド `hintChat` 機能の基本的な送受信・表示ロジック — そのまま使える

### 3-2. 変更が必要な部分（バックエンド）

- **`apps/topics`**: `KnowledgeNode.origin_node` 削除／`create_derived_node`
  削除／`build_learning_tree` の絞り込み簡略化／`serializers.py`・`admin.py`
  の `origin_node` 参照を削除
- **`apps/chat`**: `ChatSession.knowledge_node` を `OneToOneField` 化／
  `Attempt` モデルを新設／`hint_count`・`completed_at` を `ChatSession` から
  `Attempt` へ移動／`create_chat_session_for_node` を「既存があれば再利用、
  なければ作成」に変更／`record_hint_or_completion` の操作対象を `Attempt` に変更
- **`apps/reviews`**: `generate_similar_problem`・`resolve_schedule_node`
  削除／`record_review_result` の分岐条件（`origin_node` の有無での判定）を
  削除し常に `ReviewLog` を作成するよう単純化／`start_review` を「既存チャット
  セッションを取得し、新規 `Attempt` を作るだけ」に全面簡略化／
  `ReviewLog.attempt` のFK参照先変更

副次効果として、現在バグで動いていない `start_review` 経路（1-3節参照）も、
この再設計によって解消されます。

### 3-3. フロントエンドへの影響

- **`learningTree` 機能**: 現状ノードクリック時のチャット遷移がまだ実装されて
  いないため、大きな手戻りはありません。実装時は最初から「既存チャット
  セッションへの遷移」を前提に作れます。
- **`hintChat` 機能**: セッション作成・メッセージ送信の基本APIはそのまま
  使えます。「復習を開始する」ボタンなど、新しい導線のUI追加は別途必要です。

### 3-4. 新規提案: 学習木の定着度可視化

既存の `ReviewSchedule` には、経過日数と `interval_days` の比率から算出する
4段階の `retention_level`（`unlearned` / `fresh` / `fading` / `overdue`）と、
対応する `retention_color` が実装済みです。これを使い、学習木の各知識ノードを
この定着度で色分け表示する、という元々のアイデアを活かすことを提案します。

さらに、個々のノードの色分けに加えて、学習木**全体**でこの定着度の割合を
パーセンテージ表示するUIを新設し、ユーザーが「自分の学習木が今どのくらい
健康か」を一目で把握できるようにすることを提案します。復習を始めるきっかけを、
通知や強制ではなく、こうした自発的な気づきから作ることを狙います。

例（色は `ReviewSchedule.RETENTION_COLORS` の既存定義をそのまま使用、
数値は例示）:

| 定着度 | 色 | 割合(例) |
|---|---|---|
| 新鮮 (fresh) | `#2E7D46` | 40% |
| 薄れつつある (fading) | `#7FB894` | 25% |
| 要復習 (overdue) | `#C9622A` | 20% |
| 未学習 (unlearned) | `#9E9E9E` | 15% |

**実装コスト**: バックエンドの新規開発はほぼ不要（`retention_level` /
`retention_color` は既存）。主な作業は `build_learning_tree` のレスポンスに
各ノードの `retention_level` を含める拡張と、`features/learningTree` 側での
色分け・集計UIの実装です。

---

## 4. 会議で決めていただきたいこと（まとめ）

- **1節**: 廃止範囲の認識合わせ
- **2-4節 論点A**: `Attempt` テーブル新設の要否
- **2-4節 論点B**: `ChatMessage` と `Attempt` の紐付けの要否・優先度
- **2-4節 論点C**: 復習の完了基準（受動的 or 能動的）
- **3-3節**: 復習開始の新しいUI導線（ボタン配置など）の仕様
- **3-4節**: 学習木の定着度可視化（色分け・パーセンテージ表示）の採否
