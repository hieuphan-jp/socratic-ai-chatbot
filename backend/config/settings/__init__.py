# settings パッケージ
# JA: 設定は base / local / production に分割する。DJANGO_SETTINGS_MODULE で
#     どれを読むか切り替える（manage.py は既定で config.settings.local を指す）。
# VI: Cấu hình chia thành base / local / production. Dùng DJANGO_SETTINGS_MODULE
#     để chọn (manage.py mặc định trỏ tới config.settings.local).
