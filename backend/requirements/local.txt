# local.txt
# JA: ローカル開発用。base を継承し、開発ツール（lint 等）を追加する。
# VI: Dùng cho phát triển cục bộ. Kế thừa base và thêm công cụ dev (lint...).
-r base.txt
ruff~=0.14
# JA: Anthropic Claude API公式SDK (無料枠のレート制限を避けるための代替プロバイダ)
# VI: SDK chính thức của Anthropic Claude API (provider thay thế để tránh giới hạn gói miễn phí)
anthropic