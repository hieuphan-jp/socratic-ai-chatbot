# アプリ機能概要（発表スライド用ドラフト）

> 発表会（ハッカソン・チーム開発の成果発表）でのスライド構成作成のために、コードベース
> （backend/apps/{chat,topics,reviews}、frontend/src/features/{hintChat,learningTree,reviews}、
> README / CONVENTIONS.md / FOLDER_GUIDE.md 等）を調査してまとめたもの。
> **プロダクト名は未定**（リポジトリ名 `chill_education`、雛形時代の仮見出し「Team App」）。
> スライド化の際は仮に「本アプリ」と表記している箇所を正式名称に置き換えること。

---

## 1. 一言で言うと

**AIが答えを教えず「質問」でヒントを出し続けることで、ユーザー自身に考えさせながら学習を
進めさせる、対話型のAI学習支援アプリ。** 学んだ内容は自動的にツリー構造の知識ベースに
蓄積され、間隔反復（SM-2アルゴリズム）に基づいて「そろそろ復習すべきタイミング」を
リマインドしてくれる。

---

## 2. 解決したい課題・コンセプト（目的）

- 一般的なAI学習ツールは「質問すると答えを教えてくれる」ものが多いが、これだと
  ユーザーは思考せずに答えだけをコピーしがちで、学習効果が薄い。
- 本アプリのAIチューターは **「直接の答えを絶対に出さない」「ヒント・誘導質問・
  小さな問いへの分解で導く」** ことをシステムプロンプトレベルで徹底しており、
  ユーザーが自分の頭で考えるプロセスを主役に据えている（＝ソクラテス式問答法に近い設計）。
- 一方で「ユーザーが本当に理解できたか」をAIが判定するのは技術的に難しいため、
  **理解度チェックはAIにやらせず**、その分「学んだことを後から振り返れる仕組み
  （学習ツリー）」と「忘れかけた頃にもう一度触れさせる仕組み（間隔反復リマインド）」
  で学習の定着を支える、という役割分担になっている。
- 学んだ内容は使い捨てのQ&Aで終わらせず、**知識ベースとして資産化**し、後から
  検索・復習できるようにする。

---

## 3. 主な機能（3本柱）

### 3.1 ヒントチャット（AIチューター対話） — `/hint-chat`

ユーザーが自由に質問を投げかけ、AIが段階的なヒントで学習を導くコア機能。

- **答えを直接教えないAI**：システムプロンプトで「最終的な答えを絶対に出さない」
  「惜しいところまで来ていたら肯定して続きを促す」「詰まっていたら小さいサブ問題に
  分解する」ことを明示的に指示している。
- **思考ツリーへの自動整理**：会話の中の各発言を、AIが「幹（学習目的に向かう大きな
  ステップ）」か「枝（既出ステップを掘り下げる小さな質問）」かを自動判定し、木構造の
  ノードとして自動的に見出し付け（要約タイトル生成）していく。「そうですね」のような
  相槌はノード化されない。
- **分岐推定の確認UI**：どの過去ステップに続く質問なのかをAIが推定し、確信度が低い
  場合はユーザーに「この過去ノードに繋げる／今のままにする」を確認させる
  （`confirm-parent` API）。
- **チャット→知識ノード化**：フリーチャットが完了すると、AIが会話内容を要約して
  「知識ノード（学んだ内容のスナップショット）」として学習ツリーに保存する。
- **数式の表示崩れ対策**：チャット画面はプレーンテキスト表示のため、AIにLaTeX記法を
  使わせず Unicode（x², √2, π など）で書かせるルールを全プロンプト共通で徹底している
  （地味だが実際のバグ報告を踏まえた設計）。

### 3.2 学習ツリー（知識ベース） — `/learning-tree`

学んだ内容を「トピック（棚）× 知識ノード（学んだ内容そのもの）」の階層構造で
可視化・管理する機能。

- **フォルダのようなトピック階層**：トピックは自己参照の親子構造を持ち、ユーザーが
  自由に階層を作れる（例：「プログラミング」→「HTML」→「タグの書き方」）。
- **検索機能**：トピック配下を再帰的に検索し、タイトル・本文に一致する知識ノードを
  絞り込める。検索履歴も自動記録される。
- **AI検索アシスト**：「何を調べたいか正確な単語を思い出せない」ユーザー向けに、
  曖昧な説明文をAIに渡すと学習カテゴリ名の候補を1〜5個提案してくれる
  （`ai-search` API、実際の検索は行わずキーワード提案のみ）。
- **定着度の可視化**：各知識ノードは復習を完了するたびに緑が濃くなる「定着度
  （mastery_level）」を持ち、木を見ればどこが定着済みでどこが復習待ちかが一目で
  分かるようになっている（定着度と「復習期限切れかどうか」は意図的に別軸として
  UIに出している）。
- **ノードから復習へ**：知識ノードから、そのノードが生まれた元のチャットセッションに
  再度入り直して続きを対話する「復習フロー」に直結している。

### 3.3 復習リマインド（間隔反復） — `reviews`

忘却曲線に基づき、ちょうど忘れかけた頃に復習させることで学習内容の定着を狙う機能。

- **SM-2アルゴリズム**：Anki等でも使われる間隔反復アルゴリズム（SM-2）をそのまま
  実装。復習を completions するたびに「次回復習までの日数」「難易度係数」が
  動的に更新される。
- **「復習すべきもの」一覧**：復習予定日を過ぎた知識ノードを、放置期間が長い順に
  一覧表示する（`due` API）。上から順に消化すれば木の緑が早く戻る設計。
- **自己申告ではなく行動ベースの評価**：当初は「理解できたか」をユーザーに自己申告
  させる設計だったが、AIが理解度を確認しない設計方針と矛盾するため廃止。現在は
  「予定日に自分でノードを開いて完了ボタンを押したかどうか」という行動だけで
  復習の成否を判断するシンプルな設計に変更されている。

---

## 4. 技術構成（参考・スライドの技術スタック紹介用）

- **Backend**: Django 5 + Django REST Framework、セッション認証（JWT不使用）、DB は
  SQLite（本番は PostgreSQL に切替可能な設計）。
- **Frontend**: React 18 + TypeScript + Vite（全画面SPA）、サーバ状態管理は
  TanStack Query。
- **AI**: プロバイダ抽象化層を持ち、既定は APIキー不要の fake 実装（開発用）、
  `AI_PROVIDER=gemini` で Gemini（Google製LLM）に切替可能。
- **設計上の役割分担**：`views → services → models` の一方向依存を徹底し、
  複数人でのチーム開発でもレイヤーが混ざらないようにする規約（`CONVENTIONS.md`）を
  敷いている（チーム開発の成果発表という文脈で、"設計面での工夫" として触れる価値あり）。

---

## 5. スライド化にあたって確認したい／私から補足が必要な点

以下はコードからは読み取れず、実際に開発したチームの意図・体験に基づく情報なので、
スライド作成前に教えてもらえると精度が上がります。

- [ ] **プロダクト名**（未定とのことでした。決まったら教えてください）
- [ ] 開発人数・期間・役割分担（誰が何を担当したか）
- [ ] このアプリを作った直接のきっかけ・原体験（「〇〇で困っていたから」等のストーリー）
- [ ] デモで見せたい画面・操作フロー（スライドに画面キャプチャを入れるか等）
- [ ] 開発中に苦労した点・工夫した点で、特に発表で強調したいもの
  （例：チャット内容を思考ツリーに自動整理するロジック、SM-2実装、途中で復習機能を
  設計し直した経緯など、コードコメントからは "何を変えたか" は分かっても
  "苦労話としてどう語りたいか" までは分からないため）
- [ ] 今後の展望・未実装の機能で発表に含めたいもの
